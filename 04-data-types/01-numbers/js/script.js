const wholeNumber = 5;
const decimalNumber = 0.21;

//The number data type can be stored in var, let or const.
//Need not to be specified in special characters.

console.log(wholeNumber);
console.log(decimalNumber);
