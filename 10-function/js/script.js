//BLOCK OF CODE... THE EXECUTES UPON CALLING

// function declaration
function square(number){ //the *number* is a parameter
    return number * number;
}

// function call
var result = square(5); // the *5* is the argument for the parameter

console.log(result);